---
import { FaLinkedin } from 'react-icons/fa';
import ConnectionCard from './components/ConnectionCard.astro';
import { Mail, CalendarCheck } from 'lucide-react';
import styles from './ConnectSection.module.scss';
import Heading from '@/components/ui/Heading/Heading.astro';

const connections = [
  {
    icon: Mail,
    title: 'Email',
    description: 'email@example.com',
    status: 'default',
  },
  {
    icon: FaLinkedin,
    title: 'LinkedIn',
    description: 'Connect with me on LinkedIn',
    status: 'default',
  },
  {
    icon: CalendarCheck,
    title: 'Available',
    description: 'Open to opportunites\nRemote · Poland',
    status: 'available',
  },
];
---

<section class={`clipped-corners ${styles.container}`}>
  <Heading as="h2" size="small" class={styles.title}>Let's connect</Heading>
  <ul class={styles.list}>
    {
      connections.map((connection) => (
        <li>
          <ConnectionCard key={connection.title} {...connection} />
        </li>
      ))
    }
  </ul>
</section>
